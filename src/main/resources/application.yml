spring:
  datasource:
    username: root
    password: 6969
    url: jdbc:mysql://localhost:3306/movies_db
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

project:
  poster: posters/

base:
  url: "http://localhost:8080"
